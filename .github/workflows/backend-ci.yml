name: backend-ci

on:
  push:
    branches: [main, master]
  pull_request:

permissions:
  contents: read

concurrency:
  group: backend-ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  check:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true

      - name: Install dependencies
        run: uv sync --locked

      - name: Gates
        run: make check

  integration:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: ugjcs
        ports: ["5432:5432"]
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true

      - name: Install dependencies
        run: uv sync --locked

      # `alembic/env.py` builds the full `Settings` object, which has no default for
      # `jwt_secret`, so the migration steps need it even though no migration signs a
      # token. It is a throwaway value for a throwaway database; the real secret lives in
      # AWS Secrets Manager and is never held in CI.
      - name: Verify the migration applies and reverses
        env:
          UGJCS_DATABASE_URL: postgresql+asyncpg://postgres:postgres@localhost:5432/ugjcs
          UGJCS_JWT_SECRET: ci-only-not-a-real-secret-0123456789abcdef
        run: |
          uv run alembic upgrade head
          uv run alembic downgrade base
          uv run alembic upgrade head

      - name: Integration tests
        env:
          UGJCS_DATABASE_URL: postgresql+asyncpg://postgres:postgres@localhost:5432/ugjcs
          UGJCS_JWT_SECRET: ci-only-not-a-real-secret-0123456789abcdef
        run: make integration
